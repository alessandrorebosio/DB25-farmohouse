"""App configuration for the Service application."""

from django.apps import AppConfig


class ServiceConfig(AppConfig):
    """Django AppConfig for the `service` app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "service"
